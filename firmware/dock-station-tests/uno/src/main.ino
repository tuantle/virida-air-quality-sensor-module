/*
Uses a FOR loop for data and prints a number in various formats.
*/

void setup() {
    Serial.begin(9600);      // open the serial port at 9600 bps:
    delay(1000);
}

void loop() {
  Serial.println("Hello");      // prints another carriage return
  delay(1000);
}
